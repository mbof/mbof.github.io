name: Deploy SHsync to alpha

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy from hxsync'
        required: true
        default: 'alpha'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout mbof.github.io
        uses: actions/checkout@v4

      - name: Checkout hxsync
        uses: actions/checkout@v4
        with:
          repository: mbof/hxsync
          ref: ${{ github.event.inputs.branch }}
          path: hxsync-source

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: hxsync-source/package-lock.json

      - name: Install Dependencies
        run: |
          cd hxsync-source
          npm ci

      - name: Run Unit Tests
        run: |
          cd hxsync-source
          npm run test -- --watch=false --browsers=ChromeHeadless

      - name: Build Application
        run: |
          cd hxsync-source
          npm run build -- --base-href=/hx-alpha/ --configuration production

      - name: Deploy to Alpha
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: hxsync-source/dist/hxsync/browser
          target-folder: hx-alpha
